import 'package:flutter/material.dart';

class ProfilePage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Profile'),
      ),
      //Will build out the profile page soon.
      // body: Center(
      //   child: Text(
      //     'User Profile',
      //     style: TextStyle(fontSize: 24),
      //   ),
      // ),
    );
  }
}